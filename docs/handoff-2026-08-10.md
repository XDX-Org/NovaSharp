# Implementation handoff — 2026-08-10

Branch: `phase15-2`  
Head before this handoff: `84d8d22`  
Working tree: clean before adding this file

## Verified locally

`tools/qualify-release.sh` passes: warning-free Release build, valid SPDX, no Release `Microsoft.CodeAnalysis.*Features` dependency, and 127/127 tests.

## CI in progress

Run `31399505803`: <https://github.com/XDX-Org/NovaSharp/actions/runs/31399505803>

- Windows x64: passed.
- macOS arm64: passed.
- Linux x64: still running at handoff.
- macOS x64: still running at handoff.

Do not mark Phase 15.2 complete until this run, or a newer run at the same content, is fully green. If green, update `docs/delivery-plan.md` and `docs/phase-15-2-handoff.md` from 118 to 127 tests and cite run `31399505803`.

## Delivered and pushed

- Phase 15.2: exact-version semantic overlays without colour flicker; theme-controlled colours; complete negotiated LSP completion/code-action/edit fidelity; Razor-to-HTML delegation; HTML hover/completion/rename; stale-result rejection; watched files; timeout/restart/replay; transactional workspace resource edits; release-only removal of legacy Features providers; packaged assets, notices, hashes, SPDX, and four-target CI acquisition.
- Phases 3 and 9–11 were reconciled to complete in `docs/delivery-plan.md` using retained cross-platform and Linux interaction evidence.
- Phase 12: bounded DAP transport/state machine, launch/attach ownership, pinned/hash-verified netcoredbg packaging, real launch/breakpoint integration, stack/scopes/variables/evaluate, and workbench debug controls.
- Phase 13: continue/pause/step/stop, breakpoints, lazy call-stack/scope/variable views, and watch evaluation foundations.
- Phase 14: exact dirty-buffer recovery, atomic validated storage, automatic restoration safe mode, appearance/zoom/reduced-motion/high-contrast settings.
- Phase 16: manifest validation, compatibility and deny-by-default permissions/trust, bounded failure-isolated activation, public `NovaSharp.ExtensionSdk`, sample extension, and SDK/security documentation.
- Phase 17: evaluated project/configuration/framework/profile discovery, argument parsing, working-directory support, effective command model, Build Configurator UI, and atomic per-workspace persistence outside project files.
- Phase 18 foundations: preview identity, qualification script, privacy/support/limitations/release notes, checksummed four-target archives, ECDSA update-manifest verification, fixed-time artifact hashing, atomic install and rollback.

## Known blockers; do not overstate status

- Phase 12 is not complete: clean macOS runners launch and pause netcoredbg targets but source breakpoints remain pending. This is recorded in `docs/known-limitations.md`; macOS CI therefore verifies real launch/pause/stack, while Windows/Linux verify source breakpoint/evaluation.
- Phases 13–14 need the remaining documented accessibility, exception/multithread/async-stack, lifecycle-growth, and performance-budget evidence.
- Phase 16 still needs a real out-of-process extension host and install/uninstall integration; current activation coordinator proves timeout/crash containment but does not launch the sample assembly.
- Phase 17 needs environment-variable UI, launch-profile round-trip/conflict handling, and interaction/accessibility gates.
- Phase 18 is not release-qualified: native installers, signing/notarization credentials, clean-image install/update/uninstall evidence, provenance, full dependency SBOM/security review, disk-full tests, and the complete edit/build/run/debug web journey remain.

## Useful commands

```sh
git status --short
tools/qualify-release.sh
gh run view 31399505803 --json status,conclusion,jobs,url
```

Keep status evidence-based. The delivery plan deliberately leaves Phases 12–14 and 16–18 in progress.
